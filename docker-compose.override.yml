version: "3.4"

# Development environment override
services:
  php:
    volumes:
      - ./api:/var/www:rw,cached
    environment:
      APP_ENV: dev
      FRONT_URL: http://kyc:3000/check
      
  database:
    ports:
      - target: 5432
        published: 5432
        protocol: tcp